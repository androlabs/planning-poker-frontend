export type Team = {
  id: string
  name: string;
}

export type Create = {
  name: string
}

export type CreateResponse = {
  id: string;
  name: string;
}

export type UpdateTeam = {
  name: string
}
